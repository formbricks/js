{
  "name": "js",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test --no-cache",
    "test:coverage": "turbo run test:coverage --no-cache",
    "clean": "turbo run clean && rimraf node_modules .turbo coverage out"
  },
  "devDependencies": {
    "prettier": "3.8.1",
    "turbo": "2.8.20",
    "typescript": "5.9.3",
    "rimraf": "6.1.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "pnpm": {
    "overrides": {
      "vite>picomatch": "4.0.4",
      "vitest>picomatch": "4.0.4",
      "tinyglobby>picomatch": "4.0.4",
      "minimatch@10.2.4>brace-expansion": "5.0.5",
      "tar": "7.5.4",
      "@isaacs/brace-expansion": "5.0.1"
    },
    "comments": {
      "overrides": "Security fixes for transitive dependencies. Remove when upstream packages update: picomatch (Dependabot #78/#79) - awaiting upstream transitive bump; brace-expansion (Dependabot #82) - awaiting upstream transitive bump; tar (Dependabot #42) - awaiting @tailwindcss/oxide update; @isaacs/brace-expansion (Dependabot #51) - awaiting upstream transitive bump"
    }
  }
}
